//. getElementById(id)  -> It's Id not ID 
//> gets all elemnts having the specified id else null
//Note: the parameter id is case sensitive
//- If multiple elemnts with ame id's are  found it returns the first one


//. getElementsByClassName(class1 class2) 
//> gets all elements consiting of specified class names
//- returns a live array consiting of matched html array 
//- this aray is updated as the DOM changes
//> elements can be accessed via indexing starting from 0


//.getElementsByTagName(tag_name)
//> returns the elements of specified type
//- tag refers to HTML tag name
//> returns a real-timr array which upadates as per DOM changes

