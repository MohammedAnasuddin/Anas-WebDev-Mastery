Good. This is the right mindset.

If you can survive a truly difficult JS conceptual test, your understanding becomes durable instead of tutorial-dependent.

I’ll make this like a real senior-level screening:

- output prediction

- closures

- scope chain

- TDZ

- async/event loop

- promises

- execution context

- `this`

- prototype behavior

- tricky edge cases

- mental model questions

- debugging

Do NOT run the code immediately.

For each question:

1. Predict output

2. Explain WHY line by line

3. Mention JS concept involved

4. Mention whether behavior happens during:
   
   - creation phase
   
   - execution phase
   
   - call stack
   
   - microtask queue
   
   - macrotask queue

If you cannot explain WHY, you do not fully know the concept yet.

---

# RULES

- No AI

- No console execution initially

- Think deeply

- Use notebook/paper if needed

- Some questions are intentionally deceptive

- Wrong reasoning matters more than wrong output

---

# SECTION 1 — Scope & Hoisting

## Q1

```js
var a = 1;

function test() {
  console.log(a);

  var a = 2;

  function a() {}

  console.log(a);
}

test();
```

Explain:

- creation phase

- hoisting priority

- why function/var interact strangely

---

## Q2

```js
let a = 10;

{
  console.log(a);

  let a = 20;
}
```

Explain:

- TDZ

- lexical environment

- why scope lookup fails

---

## Q3

```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

Now compare with:

```js
for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

You must explain:

- closures

- shared bindings vs fresh bindings

- event loop timing

---

# SECTION 2 — Closures

## Q4

```js
function outer() {
  let count = 0;

  return {
    increment() {
      count++;
      console.log(count);
    },

    decrement() {
      count--;
      console.log(count);
    }
  };
}

const counter1 = outer();
const counter2 = outer();

counter1.increment();
counter1.increment();
counter2.increment();
counter1.decrement();
```

Explain memory behavior carefully.

---

## Q5

```js
function createFunctions() {
  const funcs = [];

  for (var i = 0; i < 3; i++) {
    funcs.push(function () {
      return i;
    });
  }

  return funcs;
}

const arr = createFunctions();

console.log(arr[0]());
console.log(arr[1]());
console.log(arr[2]());
```

Now:

- explain why

- fix WITHOUT changing `var`

- fix using IIFE

- fix using factory function

---

# SECTION 3 — Async JavaScript

## Q6

Predict EXACT output order:

```js
console.log("A");

setTimeout(() => {
  console.log("B");
}, 0);

Promise.resolve()
  .then(() => {
    console.log("C");
  })
  .then(() => {
    console.log("D");
  });

console.log("E");
```

You must explain:

- call stack

- Web APIs

- microtask queue

- macrotask queue

---

## Q7

```js
setTimeout(() => console.log(1));

Promise.resolve().then(() => {
  console.log(2);
});

queueMicrotask(() => {
  console.log(3);
});

console.log(4);
```

Explain:

- why promises beat timers

- queueMicrotask vs Promise.then

---

## Q8

```js
async function test() {
  console.log(1);

  await Promise.resolve();

  console.log(2);
}

console.log(3);

test();

console.log(4);
```

Now explain:

- what `await` actually does internally

- whether async makes function synchronous initially

- how continuation resumes

---

# SECTION 4 — Promise Traps

## Q9

```js
Promise.resolve(1)
  .then((x) => {
    console.log(x);
    return x + 1;
  })
  .then((x) => {
    throw new Error("failure");
  })
  .then((x) => {
    console.log(x);
  })
  .catch((err) => {
    console.log("caught");
  })
  .then(() => {
    console.log("after catch");
  });
```

Explain propagation carefully.

---

## Q10

```js
const p1 = Promise.resolve("A");
const p2 = new Promise((resolve) =>
  setTimeout(() => resolve("B"), 0)
);

Promise.all([p1, p2]).then(console.log);

console.log("C");
```

Explain:

- promise states

- why Promise.all waits

- microtask/macrotask interaction

---

# SECTION 5 — `this`

## Q11

```js
const obj = {
  name: "Anas",

  regular() {
    console.log(this.name);
  },

  arrow: () => {
    console.log(this.name);
  }
};

obj.regular();
obj.arrow();
```

Explain:

- lexical this

- dynamic this

- browser vs node differences

---

## Q12

```js
function User(name) {
  this.name = name;

  return {
    name: "Override"
  };
}

const u = new User("Real");

console.log(u.name);
```

Explain constructor return behavior.

---

# SECTION 6 — Prototype & Inheritance

## Q13

```js
function Person(name) {
  this.name = name;
}

Person.prototype.say = function () {
  console.log(this.name);
};

const p1 = new Person("A");
const p2 = new Person("B");

console.log(p1.say === p2.say);
```

Explain:

- prototype sharing

- memory optimization

- `__proto__`

- prototype chain lookup

---

## Q14

```js
Array.prototype.custom = function () {
  return "hello";
};

const arr = [1, 2, 3];

for (let key in arr) {
  console.log(key);
}
```

Why is this dangerous?

Explain:

- enumerable properties

- prototype pollution

- safer alternatives

---

# SECTION 7 — HARD MODE

## Q15

```js
function test() {
  console.log(a);
  console.log(foo);

  var a = 1;

  let foo = 2;

  function foo() {}

  console.log(foo);
}

test();
```

This question destroys people who memorize instead of understanding.

You must explain:

- hoisting order

- function declarations

- TDZ interaction

- why some accesses crash and some don’t

---

# FINAL THEORY QUESTIONS

## Q16

Difference between:

- execution context

- call stack

- lexical environment

- scope chain

---

## Q17

Explain event loop from scratch as if teaching a junior developer.

---

## Q18

Difference between:

- synchronous

- asynchronous

- concurrent

- parallel

in JavaScript specifically.

---

# FINAL BOSS

## Q19

Implement:

```js
myPromiseAll(promises)
```

Requirements:

- preserve order

- reject immediately

- resolve when all finish

- support non-promises

No cheating.

---

# EVALUATION CRITERIA

I will judge:

- conceptual clarity

- correctness

- debugging mindset

- mental models

- whether your understanding is shallow or deep

- whether you actually need:
  
  - YDKJS
  
  - JS internals study
  
  - async deep dives

Do not rush.

A developer who truly understands these concepts becomes dangerous in:

- React

- Node.js

- performance optimization

- debugging production issues

- interview problem solving

because most difficult frontend/backend bugs eventually reduce to:

- scope

- closures

- async timing

- state mutation

- prototype behavior

- execution order.
