# IS Palindrome

To get the last digit of number use `n%10`

> Always use Integers beware of decimal results
> To remove decimals from a Number use `Math.floor(number)`

If the output is based on a condition directly return thr condition rather than using `if-else`

```js
if(reversed=original){
    return true
}
else{
    return false
}


// Do
return reversed===original
```
